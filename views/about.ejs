<%- include('partials/header') %>
 
  <!-- Server-side slideshow (auto-initialised by public/js/slideshow.js) -->
  <section class="hero-slideshow" style="height:240px;">
    <div class="hero-slideshow-inner">
      <% 
        const hasValidSlides = slides && slides.length && slides.some(s => s && s.value && s.value.trim());
      %>
      <% if (hasValidSlides) { %>
        <% slides.forEach(function(s,i){ %>
          <% if (s && s.value && s.value.trim()) { %>
            <div class="slide"><img src="<%= s.value %>" alt="slide-<%= i %>"></div>
          <% } %>
        <% }) %>
      <% } else { %>
        <div class="slide"><img loading="lazy" src="/images/hero-illustration.svg" alt="slide-1"></div>
        <div class="slide"><img loading="lazy" src="/images/slide2.svg" alt="slide-2"></div>
        <div class="slide"><img loading="lazy" src="/images/hero-illustration.svg" alt="slide-3"></div>
      <% } %>
    </div>
  </section>

  <section class="page glass">
  <h1><%= content('about_title', 'About The Modern Pedagogues') %></h1>
  <div class="content curved">
    <p><%= content('about_body', about || 'The Modern Pedagogues was founded to bring quality home tutoring to families. Our tutors are experienced and vetted, and our lessons align with global curricula from GES to Cambridge and North American programs.') %></p>
    <h3><%= content('about_why_title', 'Why choose us?') %></h3>
    <ul>
      <li><%= content('about_why_1', 'Experienced tutors across curricula') %></li>
      <li><%= content('about_why_2', 'Tailored lesson plans') %></li>
      <li><%= content('about_why_3', 'Parent-friendly reporting and scheduling') %></li>
    </ul>
  </div>
</section>

<%- include('partials/footer') %>
